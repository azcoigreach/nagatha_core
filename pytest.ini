[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
pythonpath = .
addopts = -v --strict-markers --tb=short
markers =
    unit: unit tests
    integration: integration tests
    slow: slow tests
